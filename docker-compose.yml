services:
  warp10:
    image: warp10io/warp10:${WARP10_VERSION}
    container_name: warp10-bigdata
    ports:
      - "${WARP10_API_PORT}:8080"
      - "${WARP10_WEB_PORT}:8081"
    volumes:
      - warp10_data:/data
    restart: always

volumes:
  warp10_data:
